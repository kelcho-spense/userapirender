import { type Context } from 'hono';
import { allProfilesService, createProfileService, getProfileService } from './profile.service';

export const listProfiles = async (c: Context) => {
}

export const getProfile = async (c: Context) => {
}

export const createProfile = async (c: Context) => {
}